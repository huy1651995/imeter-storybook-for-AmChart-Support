import React from 'react';
import { TileCardProps } from './TileCard';
export interface ExpandProps {
    expand?: boolean;
    expanded?: boolean;
    expandDirection?: 'center' | 'bottom';
    expandContent?: React.ReactNode;
    onExpand?: () => void;
}
export interface IdentificationCardProps extends Omit<TileCardProps, 'data'>, ExpandProps {
    avatar: React.ReactNode;
    actions?: React.ReactNode;
    status?: React.ReactNode;
}
declare const IdentificationCard: React.FC<IdentificationCardProps>;
export default IdentificationCard;
