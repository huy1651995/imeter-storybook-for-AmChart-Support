import React from 'react';
import { SnackbarProps } from '@material-ui/core';
import { SnackbarColors } from './utils';
export declare type SnackbarStyleProps = {
    color: SnackbarColors;
};
export interface GeodeSnackbarProps extends SnackbarProps {
    color?: SnackbarColors;
    icon?: React.ReactNode;
}
declare const GeodeSnackbar: React.FC<GeodeSnackbarProps>;
export default GeodeSnackbar;
