import React from 'react';
import { BadgeProps } from '@material-ui/core';
import { StatusChipColors } from './utils';
export declare type GeodeBadgeSytleProps = {
    size: 'medium' | 'small';
    variant: 'standard' | 'dot';
    color?: StatusChipColors | 'lightDefault';
    hasChildren: boolean;
};
export interface GeodeBadgeProps extends Omit<BadgeProps, 'color' | 'overlay'> {
    size?: 'medium' | 'small';
    color?: StatusChipColors | 'lightDefault';
    className?: string;
    overlap?: 'circular' | 'rectangular';
}
declare const GeodeBadge: React.FC<GeodeBadgeProps>;
export default GeodeBadge;
