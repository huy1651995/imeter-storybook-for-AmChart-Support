import React from 'react';
import { GeodeChipProps } from './GeodeChip';
import { StatusChipColors } from './utils';
export declare type GeodeChipStyleProps = {
    lightMode: boolean;
    color: StatusChipColors;
    disabled: boolean;
};
export interface StatusChipProps extends Omit<GeodeChipProps, 'variant' | 'color' | 'size'> {
    color?: StatusChipColors;
}
declare const StatusChip: React.FC<StatusChipProps>;
export default StatusChip;
