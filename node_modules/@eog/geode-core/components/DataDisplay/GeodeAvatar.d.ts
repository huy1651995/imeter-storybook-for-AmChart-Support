import React from 'react';
import { AvatarProps } from '@material-ui/core';
export declare type GeodeAvatarStyleProps = {
    size: 'small' | 'medium' | 'large';
    color: string;
};
export interface GeodeAvatarProps extends Omit<AvatarProps, 'sizes' | 'children'> {
    size?: 'small' | 'medium' | 'large';
    icon?: React.ReactNode;
    name?: string;
    color?: string;
    className?: string;
}
declare const GeodeBadge: React.FC<GeodeAvatarProps>;
export default GeodeBadge;
