import { PropsWithChildren } from 'react';
declare type StyleProps = {
    disabled: boolean;
    error: boolean;
    className?: string;
};
export default function InputHelperText({ children, disabled, error, className, }: PropsWithChildren<StyleProps>): JSX.Element;
export {};
