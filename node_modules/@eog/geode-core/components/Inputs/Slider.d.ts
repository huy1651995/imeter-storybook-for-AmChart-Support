import React from 'react';
import { SliderProps } from '@material-ui/core';
export declare type SliderStyleProps = {
    color: 'primary' | 'secondary';
    disabled: boolean;
};
export interface GeodeSliderProps extends Omit<SliderProps, 'onChange'> {
    onChange?: (event: React.ChangeEvent<{}>, newValue: number | number[]) => void;
}
declare const GeodeSlider: React.FC<GeodeSliderProps>;
export default GeodeSlider;
