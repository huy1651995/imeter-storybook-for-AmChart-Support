import React from 'react';
import { ButtonProps } from '@material-ui/core';
export declare type GeodeBtnStyleProps = {
    disabled: boolean;
    size: string;
    variant: 'contained' | 'outlined' | 'text';
};
export interface GeodeButtonProps extends Omit<ButtonProps, 'size' | 'color'> {
    component?: React.ElementType | React.ReactNode;
    size?: 'medium' | 'small';
    color?: 'default' | 'primary' | 'secondary';
}
declare const GeodeButton: React.FC<GeodeButtonProps>;
export default GeodeButton;
